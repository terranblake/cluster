# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: auth
#   namespace: auth
#   labels:
#     app: auth
# spec:
#   replicas: 1
#   selector:
#     matchLabels:
#       app: auth
#   template:
#     metadata:
#       labels:
#         app: auth
#     spec:
#       imagePullSecrets:
#         - name: ghcr-pull-secret
#       containers:
#         - name: auth
#           resources:
#             limits:
#               cpu: 100m
#               memory: 50Mi
#           image: ghcr.io/terranblake/auth
#           imagePullPolicy: Always
#           command: ["python3", "./main.py"]
#           envFrom:
#             - secretRef:
#                 name: auth-creds
#           ports:
#             - name: http
#               containerPort: 3002
#       volumes:
#         - name: auth-creds
#           secret:
#             secretName: auth-creds
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: auth
#   namespace: auth
# spec:
#   selector:
#     app: auth
#   ports:
#     - protocol: TCP
#       port: 3002
#       name: http
#   type: ClusterIP