apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: backup-postgres
spec:
  schedule: "0 4 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            dedicated: master
          tolerations:
          - key: dedicated
            operator: Equal
            value: master
            effect: NoSchedule
          containers:
          - name: backup
            image: alpine
            args:
            - /bin/sh
            - -c
            - apk add --no-cache curl lftp;
              export TS=$(date +%s);
              mkdir -p /temp/data/;
              cp -r /data/* /temp/data;
              tar -cvf postgres_$TS.tar /temp/data;
              rm -rf /temp/data;
              lftp -u ${USER},${PASSWORD} 192.168.1.228 -e "put -O /home/cluster/ftp/backups postgres_$TS.tar;
              curl https://hc-ping.com/3baa054e-93bf-448f-a6b7-a98752145d30;
              bye
            env:
            - name: USER
              valueFrom:
                secretKeyRef:
                  name: ftp-credentials
                  key: username
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ftp-credentials
                  key: password
            volumeMounts:
            - name: data
              mountPath: /data
          restartPolicy: OnFailure
          volumes:
          - name: data
            hostPath: 
              path: /opt/postgres
              type: Directory
